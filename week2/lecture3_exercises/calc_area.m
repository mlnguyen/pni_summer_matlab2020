
function calc_area(width, len)
% function calc_area(width, len)
% 
% Calculates the area of a rectangle from the input width and length.
% Prints the area to the command line.
% Area = width * length.
%
% ARGUMENTS:
% -width: number describing width of the rectangle
% -length: number describing width of the rectangle
%
% EXAMPLE:
%   calc_area(2,3);

    area = width * len;
    disp(area);

end